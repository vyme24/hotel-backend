import type { RequestInit, Response } from "node-fetch";
import Cache from "./cache/cache";
import { IPinfoCore, IPBogon } from "./common";
export default class IPinfoCoreWrapper {
    private token;
    private baseUrl;
    private countries;
    private countriesFlags;
    private countriesCurrencies;
    private continents;
    private euCountries;
    private cache;
    private timeout;
    constructor(token: string, cache?: Cache, timeout?: number, i18nData?: {
        countries?: any;
        countriesFlags?: any;
        countriesCurrencies?: any;
        continents?: any;
        euCountries?: Array<string>;
    }, baseUrl?: string);
    static cacheKey(k: string): string;
    fetchApi(path: string, init?: RequestInit): Promise<Response>;
    lookupIp(ip?: string | undefined): Promise<IPinfoCore | IPBogon>;
    private isBogon;
}
