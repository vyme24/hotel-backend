import type { RequestInit, Response } from "node-fetch";
import Cache from "./cache/cache";
import { IPinfoLite, IPBogon } from "./common";
export default class IPinfoLiteWrapper {
    private token;
    private baseUrl;
    private euCountries;
    private cache;
    private timeout;
    constructor(token: string, cache?: Cache, timeout?: number, i18nData?: {
        euCountries?: Array<string>;
    }, baseUrl?: string);
    static cacheKey(k: string): string;
    fetchApi(path: string, init?: RequestInit): Promise<Response>;
    lookupIp(ip?: string | undefined): Promise<IPinfoLite | IPBogon>;
    private isBogon;
}
